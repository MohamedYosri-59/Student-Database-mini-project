#ifndef STD_H    // Include guard to prevent multiple inclusion of this header
#define STD_H

typedef unsigned char uint8;  // Define uint8 as alias for unsigned char
typedef char int8;            // Define int8 as alias for char
typedef unsigned short uint16; // Define uint16 as alias for unsigned short
typedef short int16;           // Define int16 as alias for short
typedef unsigned int uint32;   // Define uint32 as alias for unsigned int
typedef int int32;             // Define int32 as alias for int

typedef enum { false, true } bool; // Define a boolean enum with values false (0) and true (1)

#endif // STD_H