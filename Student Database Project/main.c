#include "SDBAPP.h" // Include application interface
#include "SDB.h" // Include application interface

int main(void) {
    SDB_APP(); // Call the application loop
    return 0;  // Return 0 to indicate success
}
